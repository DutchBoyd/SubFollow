<%= form_for(@streamer) do |f| %>
  <% if @streamer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@streamer.errors.count, "error") %> prohibited this streamer from being saved:</h2>

      <ul>
      <% @streamer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :followers %><br>
    <%= f.number_field :followers %>
  </div>
  <div class="field">
    <%= f.label :subscribers %><br>
    <%= f.number_field :subscribers %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
